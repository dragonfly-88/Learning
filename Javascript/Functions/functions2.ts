//print numbers
function printNumsUntil(num: number): void {
    for (let i: number = 0; i < num; i++) {
        if (num === 6) {
            break;
        }
        console.log(i + 1);
    }
}
printNumsUntil(10);


//function without name
const getHalf = (num: number): number => {
    return math.Round(num / 2);
}


const strNumber: string;

//parseInt - egész számot fog csinálni
//parseFloat - tizedes értéket is visszaad
console.log(gethalf(parseInt(toNumber(strNumber)));


const sum = function sumAllNumsUntillLimit(number: number): number {
    return ((1 + number) * number) / 2;
}
console.log(sum(5));


//
function funcName(params: string): string {
    return 'palacsinta';
}
